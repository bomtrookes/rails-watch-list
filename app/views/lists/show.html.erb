<h1><%= @list.name %><h1>
<ul>
  <% @list.movies.each do |movie| %>
    <li><%= movie.title %></li>
  <% end %>
</ul>

<%= link_to "Edit List Name", edit_list_path(@list, @bookmark)%>
<%= link_to "Create Bookmark", new_list_bookmark_path(@list) %>
<%# delete bookmark %>
<%# link_to "Edit Bookmark", edit_list_bookmark_path(@list)%>
<%# edit bookmark - link to edit bookmark %>
